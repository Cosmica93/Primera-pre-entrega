import Button from 'react-bootstrap/Button';

export const NavBar = ()=>{
    return(
        <div>
            menu de navegación
            <Button variant="success" size="sm">Botón de bootstrap</Button>
        </div>
    )
}